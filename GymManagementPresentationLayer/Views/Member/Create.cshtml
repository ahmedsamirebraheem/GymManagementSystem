@using GymManagementDataAccessLayer.Entities.Enums
@model GymManagementBusinessLayer.ViewModels.MemberVM.CreateVM

@{
    ViewData["Title"] = "Add Member";
}

<header class="mb-4">
    <h3 class="text-primary-color">Add New Member</h3>
    <p class="text-muted">Please fill in both personal and health information below.</p>
</header>

@if (!ViewData.ModelState.IsValid && ViewData.ModelState.ContainsKey("MissingData"))
{
    <div class="alert alert-danger">
        @Html.ValidationMessage("MissingData")
    </div>
}

<form method="post" asp-controller="Member" asp-action="CreateMember" enctype="multipart/form-data">

    <div class="card shadow-sm border-0 mb-4">
        <div class="card-header bg-primary text-white py-3">
            <h5 class="mb-0"><i class="bi bi-person-badge me-2"></i>Personal & Address Information</h5>
        </div>
        <div class="card-body p-4">
            <div class="row g-4">

                <div class="col-12 border-bottom pb-4 mb-2">
                    <label asp-for="PhotoFile" class="form-label fw-bold">Profile Photo</label>
                    <div class="d-flex align-items-start gap-4">
                        <div class="flex-shrink-0">
                            <img id="photoPreview"
                                 src="/images/User.jpg"
                                 alt="Preview"
                                 style="width: 120px; height: 120px; object-fit: cover;"
                                 class="rounded-circle img-thumbnail shadow-sm" />
                        </div>
                        <div class="flex-grow-1">
                            <input asp-for="PhotoFile" type="file" class="form-control mb-2" accept="image/*" id="photoInput" />
                            <span asp-validation-for="PhotoFile" class="text-danger small"></span>
                            <small class="form-text text-muted d-block">Max size: 5 MB. Allowed: JPG, PNG, JPEG</small>
                        </div>
                    </div>
                </div>

                <div class="col-md-6">
                    <label asp-for="Name" class="form-label fw-bold">Full Name</label>
                    <input asp-for="Name" class="form-control" placeholder="Enter full name" />
                    <span asp-validation-for="Name" class="text-danger small"></span>
                </div>

                <div class="col-md-6">
                    <label asp-for="Email" class="form-label fw-bold">Email Address</label>
                    <input asp-for="Email" class="form-control" placeholder="example@domain.com" />
                    <span asp-validation-for="Email" class="text-danger small"></span>
                </div>

                <div class="col-md-4">
                    <label asp-for="PhoneNumber" class="form-label fw-bold">Phone Number</label>
                    <input asp-for="PhoneNumber" class="form-control" placeholder="01xxxxxxxxx" />
                    <span asp-validation-for="PhoneNumber" class="text-danger small"></span>
                </div>

                <div class="col-md-4">
                    <label asp-for="DateOfBirth" class="form-label fw-bold">Date of Birth</label>
                    <input asp-for="DateOfBirth" class="form-control" type="date" />
                    <span asp-validation-for="DateOfBirth" class="text-danger small"></span>
                </div>

                <div class="col-md-4">
                    <label asp-for="Gender" class="form-label fw-bold">Gender</label>
                    <select asp-for="Gender" asp-items="Html.GetEnumSelectList<Gender>()" class="form-select">
                        <option value="">-- Select Gender --</option>
                    </select>
                    <span asp-validation-for="Gender" class="text-danger small"></span>
                </div>

                <div class="col-md-3">
                    <label asp-for="BuildingNumber" class="form-label fw-bold">Building No.</label>
                    <input asp-for="BuildingNumber" class="form-control" />
                    <span asp-validation-for="BuildingNumber" class="text-danger small"></span>
                </div>

                <div class="col-md-5">
                    <label asp-for="Street" class="form-label fw-bold">Street</label>
                    <input asp-for="Street" class="form-control" />
                    <span asp-validation-for="Street" class="text-danger small"></span>
                </div>

                <div class="col-md-4">
                    <label asp-for="City" class="form-label fw-bold">City</label>
                    <input asp-for="City" class="form-control" />
                    <span asp-validation-for="City" class="text-danger small"></span>
                </div>
            </div>
        </div>
    </div>

    <div class="card shadow-sm border-0">
        <div class="card-header bg-success text-white py-3">
            <h5 class="mb-0"><i class="bi bi-heart-pulse me-2"></i>Health Record</h5>
        </div>
        <div class="card-body p-4">
            <div class="row g-4">
                <div class="col-md-4">
                    <label asp-for="HealthRecord.Height" class="form-label fw-bold">Height (cm)</label>
                    <div class="input-group">
                        <input asp-for="HealthRecord.Height" class="form-control" />
                        <span class="input-group-text">cm</span>
                    </div>
                    <span asp-validation-for="HealthRecord.Height" class="text-danger small"></span>
                </div>

                <div class="col-md-4">
                    <label asp-for="HealthRecord.Weight" class="form-label fw-bold">Weight (kg)</label>
                    <div class="input-group">
                        <input asp-for="HealthRecord.Weight" class="form-control" />
                        <span class="input-group-text">kg</span>
                    </div>
                    <span asp-validation-for="HealthRecord.Weight" class="text-danger small"></span>
                </div>

                <div class="col-md-4">
                    <label asp-for="HealthRecord.BloodType" class="form-label fw-bold">Blood Type</label>
                    <select asp-for="HealthRecord.BloodType" class="form-select">
                        <option value="">Select Type</option>
                        <option>A+</option>
                        <option>A-</option>
                        <option>B+</option>
                        <option>B-</option>
                        <option>O+</option>
                        <option>O-</option>
                        <option>AB+</option>
                        <option>AB-</option>
                    </select>
                    <span asp-validation-for="HealthRecord.BloodType" class="text-danger small"></span>
                </div>

                <div class="col-12">
                    <label asp-for="HealthRecord.Note" class="form-label fw-bold">Medical Notes</label>
                    <textarea asp-for="HealthRecord.Note" class="form-control" rows="3" placeholder="Any allergies, injuries, or chronic conditions..."></textarea>
                    <span asp-validation-for="HealthRecord.Note" class="text-danger small"></span>
                </div>
            </div>
        </div>
    </div>

    <footer class="d-flex justify-content-end gap-3 mt-5 pb-5">
        <a asp-controller="Member" asp-action="Index" class="btn btn-outline-secondary px-5 py-2">
            Cancel
        </a>
        <button type="submit" class="btn btn-primary px-5 py-2">
            <i class="bi bi-plus-circle me-2"></i> Save Member
        </button>
    </footer>

</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <script>
        const photoInput = document.getElementById('photoInput');
        const photoPreview = document.getElementById('photoPreview');
        const defaultSrc = "/images/User.jpg";

        photoInput.onchange = function (evt) {
            const [file] = this.files;
            if (file) {
                // Revoke old URL to avoid memory leaks
                if (photoPreview.src.startsWith('blob:')) {
                    URL.revokeObjectURL(photoPreview.src);
                }
                photoPreview.src = URL.createObjectURL(file);
            } else {
                photoPreview.src = defaultSrc;
            }
        };
    </script>
}